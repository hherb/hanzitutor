{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default permissions for the main window. Commands defined by this app are always callable; this covers the Tauri core plugins.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
